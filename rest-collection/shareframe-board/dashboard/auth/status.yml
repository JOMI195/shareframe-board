info:
  name: status
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/auth/status"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
