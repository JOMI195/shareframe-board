info:
  name: logout
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/auth/logout"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
