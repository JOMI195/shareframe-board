info:
  name: mode
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/connection/mode"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
