info:
  name: status
  type: http
  seq: 1

http:
  method: GET
  url: "http://localhost:8080/api/connection/status"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
