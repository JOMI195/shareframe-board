info:
  name: saved-networks
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/connection/saved-networks"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
