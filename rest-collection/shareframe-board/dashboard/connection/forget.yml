info:
  name: forget
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/connection/forget"
  headers:
    Cookie: "session={{sessionCookie}}"
  body:
    type: json
    data: |-
      {
        "ssid": "MyNetwork"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
