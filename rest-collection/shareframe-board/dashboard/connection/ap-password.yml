info:
  name: ap-password
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/connection/ap-password"
  headers:
    Cookie: "session={{sessionCookie}}"
  body:
    type: json
    data: |-
      {
        "password": "newappassword"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
