info:
  name: info
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/system/info"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
