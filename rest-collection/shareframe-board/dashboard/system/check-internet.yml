info:
  name: check-internet
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/system/check-internet"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
