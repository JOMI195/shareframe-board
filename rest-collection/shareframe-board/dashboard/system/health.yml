info:
  name: health
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/system/health"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
