info:
  name: update-status
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/system/updates/status"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
