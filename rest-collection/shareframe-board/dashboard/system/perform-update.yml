info:
  name: perform-update
  type: http
  seq: 6

http:
  method: POST
  url: "http://localhost:8080/api/system/updates/perform-update"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
