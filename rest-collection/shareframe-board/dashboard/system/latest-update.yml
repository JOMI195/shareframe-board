info:
  name: latest-update
  type: http
  seq: 5

http:
  method: GET
  url: "http://localhost:8080/api/system/updates/latest"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
