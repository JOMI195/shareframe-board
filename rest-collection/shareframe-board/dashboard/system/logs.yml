info:
  name: logs
  type: http
  seq: 6

http:
  method: GET
  url: "http://localhost:8080/api/system/logs?service_name=shareframe.service&lines=100"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
