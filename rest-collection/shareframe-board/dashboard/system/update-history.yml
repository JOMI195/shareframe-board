info:
  name: update-history
  type: http
  seq: 8

http:
  method: GET
  url: "http://localhost:8080/api/system/updates/history"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
