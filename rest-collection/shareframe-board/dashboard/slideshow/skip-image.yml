info:
  name: skip-image
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/frame/slideshow/skip"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
