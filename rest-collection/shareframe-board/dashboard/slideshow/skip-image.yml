info:
  name: skip-image
  type: http
  seq: 1

http:
  method: POST
  url: "http://localhost:8080/api/frame/slideshow/skip-slideshow-image"
  headers:
    Cookie: "session={{sessionCookie}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
