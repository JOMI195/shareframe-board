debug = true
production = false
base_url = ""

[log]
log_path = "logs"

[shareframe_application]
service_name = "shareframe.service"
log_file = "shareframe-application.log"

[database]
database_path = "db"
database_name = "shareframe.db"
migrations_path = "migrations"

[auth_token]
http_fetch_token_url = "/api/frames/obtain-frame-auth-token/"
http_verify_token_url = "/api/frames/verify-frame-auth-token/"

[websocket]
ws_path = "ws/frames/"

[image]
image_save_path = "images"

[display]

[update]

[dashboard]

[heartbeat]
interval_secs = 60

[config_sender]
interval_secs = 900

[image_check]
interval_secs = 900
