debug = true
production = false
base_url = ""

[log]
log_path = "logs"

[shareframe_application]
service_name = "shareframe.service"
log_file = "shareframe-application.log"

[database]
database_path = "db"
database_name = "shareframe.db"
migrations_path = "migrations"

[auth_token]
http_fetch_token_url = "/api/frames/obtain-frame-auth-token/"
http_verify_token_url = "/api/frames/verify-frame-auth-token/"

[websocket]
ws_path = "ws/frames/"

[image]
image_save_path = "images"

[display]
mock_display = false
interval_secs = 900
min_refresh_secs = 120
loading_image_path = "static/image/loading"
default_images_path = "static/image/default"

[update]

[dashboard_application]
service_name = "shareframe-dashboard.service"
log_file = "shareframe-dashboard.log"
socket_path = "/tmp/shareframe-ipc.sock"
port = 8080
host = "0.0.0.0"
http_verify_otp_url = "/api/frames/verify-frame-otp/"

[heartbeat]
interval_secs = 60

[config_sender]
interval_secs = 900

[image_check]
interval_secs = 900
